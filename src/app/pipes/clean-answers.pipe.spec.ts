import { CleanAnswersPipe } from './clean-answers.pipe';

describe('CleanAnswersPipe', () => {
  it('create an instance', () => {
    const pipe = new CleanAnswersPipe();
    expect(pipe).toBeTruthy();
  });
});
